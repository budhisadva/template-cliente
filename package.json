{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rollup --config --watch",
    "style": "sass --watch sass/index.sass:public/bundle.css"
  },
  "type": "module",
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rollup": "^4.13.0"
  },
  "dependencies": {
    "node-sass": "^9.0.0"
  }
}
